---
interface Props {
  youtubeUrl?: string;
  linkedinUrl?: string;
  cfeUrl?: string;
  twitchUrl?: string;
}

const { youtubeUrl, linkedinUrl, cfeUrl, twitchUrl } = Astro.props;
---

<div>
  Watch live on:
  {
    cfeUrl ? (
      <ul class="flex gap-4">
        <li>
          <a href={cfeUrl}>CFE.dev</a>
        </li>
      </ul>
    ) : (
      <ul class="flex gap-4">
        {twitchUrl ? (
          <li>
            <a href={twitchUrl}>Twitch</a>
          </li>
        ) : null}
        {youtubeUrl ? (
          <li>
            <a href={youtubeUrl}>YouTube</a>
          </li>
        ) : null}
        {linkedinUrl ? (
          <li>
            <a href={linkedinUrl}>LinkedIn</a>
          </li>
        ) : null}
      </ul>
    )
  }
</div>
<style>
  a:not(class) {
    text-decoration: underline;
  }
</style>
